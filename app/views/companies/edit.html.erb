<% provide(:title, "Edit #{@company.name}") %>
<% provide(:body_class, "company-page") %>

<div class="container admin-container">
<h1 class="header">Editing Company</h1>

<%= render 'form', company: @company %>
</div>